{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in *.png|*.jpg|*.jpeg|*.webp) node scripts/compress-image.mjs \"$f\" 2>/dev/null || true ;; esac"
          }
        ]
      }
    ]
  }
}
